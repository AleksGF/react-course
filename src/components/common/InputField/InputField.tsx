import React from 'react';

class InputField extends React.Component<undefined, undefined> {
  render() {
    return <input type={'text'} placeholder={'placeholder'} value={''} />;
  }
}

export default InputField;
